PyQt6>=6.0.0,<7.0.0
# Add other dependencies needed for running main.py and 文件合并2PDF.py
# e.g., selenium, undetected-chromedriver, pillow
# It's best to have the GUI depend on the core requirements.txt
# Or list them here if this GUI is meant to be standalone.
# 你原有项目中 requirements.txt 的内容也应该在这里或者确保已安装
# 例如：
# selenium
# undetected-chromedriver
# Pillow
